# 项目历史

## 干净基线

仓库由已运行的六模块引擎提取而来，上游快照为 `2ac477a`。公开基线提交为
`21b862a`，标签为 [baseline-six-modules](https://github.com/haoran3160-afk/AI_Daily_workflow/tree/baseline-six-modules)。

基线保留实际运行代码、必要依赖、可独立运行测试和 MIT 许可。没有复制原仓库全部历史、
私有配置、笔记、模型输出或开发聊天；原工作区及其历史保留。

## 日报与周报分离

`489e877` 对应两模块轮换日报和六模块周报：周一至周六每天两个主题，周日汇总已发布材料。
保留独立审核、日期命名、同名保护、证据边界与可恢复发布。

## 仓库呈现整理

根目录的抓取与基础契约模块归入 `src/pkm_workflow`，同步内部导入与测试；
`scripts/run_workflow.py` 入口不变。README 面向首次访问者重写，配置、开发与历史独立成文。
本次不改变阅读节奏、模型配置、当前本地部署或任何 Vault 笔记。
